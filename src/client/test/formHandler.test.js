import { onSubmit } from "../js/formHandler"

describe("Testing onSubmit functionality", () => {
    test("Testing the function declaration", () => {
        expect(onSubmit).toBeDefined();
    })
});